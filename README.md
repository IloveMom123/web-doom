# web-DOOM




Web DOOM is a html script that runs in the browser that runs the "doom.wad" file if found in the same directory as the html file.

ps: For Developers, we added a wad viewer so you can inspect sertan elements such as the floor
